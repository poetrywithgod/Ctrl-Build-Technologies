<section class="cb-work" id="work">
  <div class="cb-container">
    <div class="cb-work__header">
      <div>
        <p class="cb-eyebrow">/ SELECTED WORK</p>
        <h2>Software built for <span>real-world problems.</span></h2>
      </div>

      <div class="cb-work__header-side">
        <p>
          A selection of systems, platforms and digital products engineered
          around specific business and operational needs.
        </p>

        <a href="/work/">
          View all work
          <span aria-hidden="true">↗</span>
        </a>
      </div>
    </div>

    <div class="cb-work__grid">
      <a class="cb-work-card cb-work-card--featured" href="/work/">
        <div class="cb-work-card__visual">
          <div class="cb-work-card__window">
            <div class="cb-work-card__window-bar">
              <span></span>
              <span></span>
              <span></span>
              <small>CTRLBUILD / EDUCATION PLATFORM</small>
            </div>

            <div class="cb-work-card__dashboard">
              <div class="cb-work-card__dashboard-sidebar">
                <span></span>
                <span></span>
                <span></span>
                <span></span>
              </div>

              <div class="cb-work-card__dashboard-main">
                <div class="cb-work-card__metric-row">
                  <span></span>
                  <span></span>
                  <span></span>
                </div>

                <div class="cb-work-card__chart">
                  <i></i>
                  <i></i>
                  <i></i>
                  <i></i>
                  <i></i>
                  <i></i>
                </div>

                <div class="cb-work-card__rows">
                  <span></span>
                  <span></span>
                  <span></span>
                </div>
              </div>
            </div>
          </div>

          <span class="cb-work-card__visual-label">01 / PLATFORM</span>
        </div>

        <div class="cb-work-card__content">
          <div>
            <p class="cb-work-card__type">Education · SaaS · Operations</p>
            <h3>Connected school management infrastructure.</h3>
            <p>
              A multi-tenant education platform bringing academic operations,
              communication, assessment and school workflows into one system.
            </p>
          </div>

          <span class="cb-work-card__arrow" aria-hidden="true">↗</span>
        </div>

        <div class="cb-work-card__tags">
          <span>React</span>
          <span>TypeScript</span>
          <span>Supabase</span>
          <span>AI</span>
        </div>
      </a>

      <a class="cb-work-card" href="/work/">
        <div class="cb-work-card__visual cb-work-card__visual--system">
          <div class="cb-work-card__system-grid">
            <span></span>
            <span></span>
            <span></span>
            <span></span>
            <span></span>
            <span></span>
          </div>

          <div class="cb-work-card__system-core">
            <span>CTRL</span>
            <strong>SYSTEM</strong>
          </div>

          <span class="cb-work-card__visual-label">02 / BUSINESS SYSTEM</span>
        </div>

        <div class="cb-work-card__content">
          <div>
            <p class="cb-work-card__type">Business · Operations · Workflow</p>
            <h3>Business processes turned into software.</h3>
            <p>
              Custom operational systems that connect people, data and
              workflows around how an organisation actually works.
            </p>
          </div>

          <span class="cb-work-card__arrow" aria-hidden="true">↗</span>
        </div>

        <div class="cb-work-card__tags">
          <span>Web Apps</span>
          <span>APIs</span>
          <span>Dashboards</span>
        </div>
      </a>

      <a class="cb-work-card" href="/work/">
        <div class="cb-work-card__visual cb-work-card__visual--analytics">
          <div class="cb-work-card__analytics-head">
            <span>BUSINESS INTELLIGENCE</span>
            <strong>+24.8%</strong>
          </div>

          <div class="cb-work-card__analytics-chart">
            <span></span>
            <span></span>
            <span></span>
            <span></span>
            <span></span>
            <span></span>
            <span></span>
          </div>

          <div class="cb-work-card__analytics-axis">
            <i></i>
            <i></i>
            <i></i>
            <i></i>
          </div>

          <span class="cb-work-card__visual-label">03 / DATA &amp; ANALYTICS</span>
        </div>

        <div class="cb-work-card__content">
          <div>
            <p class="cb-work-card__type">Analytics · Reporting · Intelligence</p>
            <h3>Data made useful for better operations.</h3>
            <p>
              Dashboards and reporting systems that transform operational data
              into clear, accessible business intelligence.
            </p>
          </div>

          <span class="cb-work-card__arrow" aria-hidden="true">↗</span>
        </div>

        <div class="cb-work-card__tags">
          <span>Analytics</span>
          <span>Visualisation</span>
          <span>Reporting</span>
        </div>
      </a>
    </div>

    <div class="cb-work__footer">
      <span class="cb-work__footer-line"></span>
      <p>
        We don't build software to fill a portfolio.
        <strong>We build it to solve something.</strong>
      </p>
    </div>
  </div>
</section>

<style>
  .cb-work {
    position: relative;
    padding: 8rem 0;
    border-top: 1px solid var(--color-border);
    background: var(--color-bg);
  }

  .cb-work__header {
    display: grid;
    gap: 2rem;
    margin-bottom: 4rem;
  }

  .cb-work__header h2 {
    max-width: 12ch;
    margin-top: 1rem;
    font-size: clamp(2.7rem, 6vw, 5rem);
    line-height: 0.98;
    letter-spacing: -0.06em;
  }

  .cb-work__header h2 span {
    color: var(--color-brand-coral);
  }

  .cb-work__header-side {
    display: grid;
    align-content: end;
    gap: 1.5rem;
    max-width: 31rem;
  }

  .cb-work__header-side p {
    color: var(--color-text-muted);
    font-size: 1rem;
    line-height: 1.8;
  }

  .cb-work__header-side a {
    display: inline-flex;
    width: fit-content;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-brand-coral);
    font-size: 0.85rem;
    font-weight: 700;
  }

  .cb-work__header-side a span {
    transition: transform 180ms ease;
  }

  .cb-work__header-side a:hover span {
    transform: translate(3px, -3px);
  }

  .cb-work__grid {
    display: grid;
    gap: 1rem;
  }

  .cb-work-card {
    display: grid;
    overflow: hidden;
    border: 1px solid var(--color-border);
    background: var(--color-bg-subtle);
    color: var(--color-text);
    transition:
      border-color 180ms ease,
      transform 180ms ease;
  }

  .cb-work-card:hover {
    z-index: 1;
    border-color: color-mix(
      in srgb,
      var(--color-brand-coral) 45%,
      var(--color-border)
    );
    transform: translateY(-4px);
  }

  .cb-work-card__visual {
    position: relative;
    min-height: 19rem;
    overflow: hidden;
    border-bottom: 1px solid var(--color-border);
    background:
      linear-gradient(
        135deg,
        color-mix(in srgb, var(--color-brand-burgundy) 14%, var(--color-bg)),
        var(--color-bg-subtle)
      );
  }

  .cb-work-card__visual::before {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(
        color-mix(in srgb, var(--color-border) 55%, transparent) 1px,
        transparent 1px
      ),
      linear-gradient(
        90deg,
        color-mix(in srgb, var(--color-border) 55%, transparent) 1px,
        transparent 1px
      );
    background-size: 2.5rem 2.5rem;
    content: '';
    opacity: 0.45;
  }

  .cb-work-card__window {
    position: absolute;
    top: 2rem;
    right: 2rem;
    bottom: 0;
    left: 2rem;
    overflow: hidden;
    border: 1px solid var(--color-border);
    border-bottom: 0;
    background: var(--color-bg);
    box-shadow: 0 1.5rem 4rem color-mix(in srgb, #000 16%, transparent);
    transition: transform 220ms ease;
  }

  .cb-work-card:hover .cb-work-card__window {
    transform: translateY(-0.4rem);
  }

  .cb-work-card__window-bar {
    display: flex;
    height: 2.2rem;
    align-items: center;
    gap: 0.35rem;
    padding: 0 0.75rem;
    border-bottom: 1px solid var(--color-border);
  }

  .cb-work-card__window-bar > span {
    width: 0.35rem;
    height: 0.35rem;
    border-radius: 50%;
    background: var(--color-brand-coral);
  }

  .cb-work-card__window-bar small {
    margin-left: auto;
    color: var(--color-text-subtle);
    font-family: var(--font-mono);
    font-size: 0.5rem;
    letter-spacing: 0.05em;
  }

  .cb-work-card__dashboard {
    display: grid;
    height: calc(100% - 2.2rem);
    grid-template-columns: 3.5rem 1fr;
  }

  .cb-work-card__dashboard-sidebar {
    display: grid;
    align-content: start;
    gap: 0.7rem;
    padding: 1rem 0.8rem;
    border-right: 1px solid var(--color-border);
  }

  .cb-work-card__dashboard-sidebar span {
    display: block;
    height: 0.3rem;
    border-radius: 999px;
    background: var(--color-border);
  }

  .cb-work-card__dashboard-sidebar span:first-child {
    background: var(--color-brand-coral);
  }

  .cb-work-card__dashboard-main {
    padding: 1rem;
  }

  .cb-work-card__metric-row {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.6rem;
  }

  .cb-work-card__metric-row span {
    height: 2.7rem;
    border: 1px solid var(--color-border);
    background: var(--color-bg-subtle);
  }

  .cb-work-card__chart {
    display: flex;
    height: 7rem;
    align-items: end;
    gap: 0.7rem;
    margin-top: 1rem;
    padding: 0.8rem;
    border: 1px solid var(--color-border);
  }

  .cb-work-card__chart i {
    flex: 1;
    min-height: 15%;
    background: var(--color-brand-coral);
    opacity: 0.7;
  }

  .cb-work-card__chart i:nth-child(2) {
    min-height: 35%;
  }

  .cb-work-card__chart i:nth-child(3) {
    min-height: 25%;
  }

  .cb-work-card__chart i:nth-child(4) {
    min-height: 60%;
  }

  .cb-work-card__chart i:nth-child(5) {
    min-height: 45%;
  }

  .cb-work-card__chart i:nth-child(6) {
    min-height: 78%;
  }

  .cb-work-card__rows {
    display: grid;
    gap: 0.5rem;
    margin-top: 0.8rem;
  }

  .cb-work-card__rows span {
    height: 0.45rem;
    background: var(--color-border);
  }

  .cb-work-card__rows span:nth-child(2) {
    width: 82%;
  }

  .cb-work-card__rows span:nth-child(3) {
    width: 65%;
  }

  .cb-work-card__visual-label {
    position: absolute;
    right: 1rem;
    bottom: 1rem;
    z-index: 1;
    color: var(--color-text-subtle);
    font-family: var(--font-mono);
    font-size: 0.58rem;
    letter-spacing: 0.08em;
  }

  .cb-work-card__content {
    display: flex;
    justify-content: space-between;
    gap: 1.5rem;
    padding: 1.5rem;
  }

  .cb-work-card__type {
    margin-bottom: 0.8rem;
    color: var(--color-brand-coral);
    font-family: var(--font-mono);
    font-size: 0.62rem;
    font-weight: 600;
    letter-spacing: 0.07em;
    text-transform: uppercase;
  }

  .cb-work-card h3 {
    max-width: 28rem;
    font-size: clamp(1.45rem, 3vw, 2.1rem);
    line-height: 1.05;
    letter-spacing: -0.035em;
  }

  .cb-work-card__content > div > p:last-child {
    max-width: 32rem;
    margin-top: 1rem;
    color: var(--color-text-muted);
    font-size: 0.88rem;
    line-height: 1.7;
  }

  .cb-work-card__arrow {
    flex-shrink: 0;
    color: var(--color-brand-coral);
    font-size: 1.25rem;
    transition: transform 180ms ease;
  }

  .cb-work-card:hover .cb-work-card__arrow {
    transform: translate(3px, -3px);
  }

  .cb-work-card__tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    padding: 0 1.5rem 1.5rem;
  }

  .cb-work-card__tags span {
    padding: 0.4rem 0.6rem;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    color: var(--color-text-muted);
    font-family: var(--font-mono);
    font-size: 0.6rem;
  }

  .cb-work-card__visual--system {
    display: grid;
    place-items: center;
    background:
      radial-gradient(
        circle at center,
        color-mix(in srgb, var(--color-brand-coral) 11%, transparent),
        transparent 48%
      ),
      var(--color-bg-subtle);
  }

  .cb-work-card__system-grid {
    position: absolute;
    inset: 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.75rem;
    padding: 2rem;
  }

  .cb-work-card__system-grid span {
    border: 1px solid var(--color-border);
  }

  .cb-work-card__system-core {
    position: relative;
    z-index: 1;
    display: grid;
    width: 8rem;
    height: 8rem;
    place-items: center;
    align-content: center;
    border: 1px solid var(--color-brand-coral);
    border-radius: 50%;
    background: var(--color-bg);
    box-shadow:
      0 0 0 1rem color-mix(in srgb, var(--color-brand-coral) 5%, transparent),
      0 1.5rem 4rem color-mix(in srgb, #000 14%, transparent);
  }

  .cb-work-card__system-core span {
    color: var(--color-brand-coral);
    font-family: var(--font-mono);
    font-size: 0.65rem;
    letter-spacing: 0.1em;
  }

  .cb-work-card__system-core strong {
    font-size: 1.15rem;
    letter-spacing: -0.04em;
  }

  .cb-work-card__visual--analytics {
    padding: 2rem;
    background:
      linear-gradient(
        145deg,
        color-mix(in srgb, var(--color-brand-burgundy) 9%, var(--color-bg-subtle)),
        var(--color-bg-subtle)
      );
  }

  .cb-work-card__analytics-head {
    position: relative;
    z-index: 1;
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    font-family: var(--font-mono);
  }

  .cb-work-card__analytics-head span {
    color: var(--color-text-subtle);
    font-size: 0.58rem;
    letter-spacing: 0.08em;
  }

  .cb-work-card__analytics-head strong {
    color: var(--color-brand-coral);
    font-size: 0.8rem;
  }

  .cb-work-card__analytics-chart {
    position: absolute;
    right: 2rem;
    bottom: 3rem;
    left: 2rem;
    height: 9rem;
    border-bottom: 1px solid var(--color-border);
  }

  .cb-work-card__analytics-chart span {
    position: absolute;
    bottom: 0;
    display: block;
    width: 12%;
    border-top: 2px solid var(--color-brand-coral);
    transform-origin: left center;
  }

  .cb-work-card__analytics-chart span:nth-child(1) {
    left: 0;
    transform: rotate(-4deg);
  }

  .cb-work-card__analytics-chart span:nth-child(2) {
    left: 12%;
    transform: rotate(8deg);
  }

  .cb-work-card__analytics-chart span:nth-child(3) {
    left: 24%;
    transform: rotate(-12deg);
  }

  .cb-work-card__analytics-chart span:nth-child(4) {
    left: 36%;
    transform: rotate(5deg);
  }

  .cb-work-card__analytics-chart span:nth-child(5) {
    left: 48%;
    transform: rotate(-8deg);
  }

  .cb-work-card__analytics-chart span:nth-child(6) {
    left: 60%;
    transform: rotate(10deg);
  }

  .cb-work-card__analytics-chart span:nth-child(7) {
    left: 72%;
    transform: rotate(-3deg);
  }

  .cb-work-card__analytics-axis {
    position: absolute;
    right: 2rem;
    bottom: 1rem;
    left: 2rem;
    display: flex;
    justify-content: space-between;
  }

  .cb-work-card__analytics-axis i {
    width: 0.25rem;
    height: 0.25rem;
    border-radius: 50%;
    background: var(--color-border);
  }

  .cb-work__footer {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-top: 2.5rem;
  }

  .cb-work__footer-line {
    width: 3rem;
    height: 1px;
    flex-shrink: 0;
    background: var(--color-brand-coral);
  }

  .cb-work__footer p {
    font-family: var(--font-display);
    font-size: clamp(1rem, 2vw, 1.25rem);
  }

  .cb-work__footer strong {
    color: var(--color-brand-coral);
  }

  @media (min-width: 700px) {
    .cb-work {
      padding: 9rem 0;
    }

    .cb-work__header {
      grid-template-columns: minmax(0, 1fr) minmax(18rem, 0.65fr);
      align-items: end;
    }

    .cb-work__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .cb-work-card--featured {
      grid-column: 1 / -1;
    }

    .cb-work-card--featured .cb-work-card__visual {
      min-height: 26rem;
    }

    .cb-work-card--featured .cb-work-card__content {
      padding: 2rem;
    }

    .cb-work-card--featured .cb-work-card__tags {
      padding-right: 2rem;
      padding-bottom: 2rem;
      padding-left: 2rem;
    }
  }

  @media (min-width: 1100px) {
    .cb-work {
      padding: 10rem 0;
    }

    .cb-work__grid {
      grid-template-columns: 1.25fr 0.75fr;
      gap: 1.25rem;
    }

    .cb-work-card--featured {
      grid-column: 1 / -1;
    }

    .cb-work-card--featured .cb-work-card__visual {
      min-height: 31rem;
    }

    .cb-work-card__visual {
      min-height: 23rem;
    }

    .cb-work-card__content {
      padding: 2rem;
    }

    .cb-work-card__tags {
      padding-right: 2rem;
      padding-bottom: 2rem;
      padding-left: 2rem;
    }
  }

  @media (max-width: 520px) {
    .cb-work {
      padding: 6rem 0;
    }

    .cb-work-card__visual {
      min-height: 16rem;
    }

    .cb-work-card__window {
      top: 1.25rem;
      right: 1.25rem;
      left: 1.25rem;
    }

    .cb-work-card__content {
      padding: 1.25rem;
    }

    .cb-work-card__tags {
      padding: 0 1.25rem 1.25rem;
    }

    .cb-work-card__visual--analytics {
      padding: 1.25rem;
    }

    .cb-work-card__analytics-chart {
      right: 1.25rem;
      left: 1.25rem;
    }

    .cb-work-card__analytics-axis {
      right: 1.25rem;
      left: 1.25rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .cb-work-card,
    .cb-work-card__window,
    .cb-work-card__arrow,
    .cb-work-card__header-side a span {
      transition: none;
    }

    .cb-work-card:hover {
      transform: none;
    }

    .cb-work-card:hover .cb-work-card__window {
      transform: none;
    }
  }
</style>
